%p
 = link_to_remote 'Nueva Cita', :url => {:action => :new}, :before => "$('#loading').show()", :complete => "$('#loading').hide()"
%p
 #calendar
 #create_event_dialog{ :stylefood => 'display:none' }
  #create_event
