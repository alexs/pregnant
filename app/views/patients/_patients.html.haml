.patients_result
  - @patients.each do |patient|
    %table
      %tr{:class => "#{cycle ('odd', 'even')}"}
        %td.name
          = patient.fullname
        %td.age
          = patient.age.to_s + " " + "años"
        %td.address
          = patient.full_address
        %td.actions
          acciones
%br
Total de registros encontrados: #{@patients.size}
